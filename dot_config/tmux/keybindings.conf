# reload config file
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

bind-key ? display-popup -E 'tmux list-keys | fzf --reverse --ansi | cut -f 2 | xargs -I % tmux display-message "%"'
# rebind Prefix Key
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Unbind Meta-left and Meta-right keys since they're more often
# used for quickly jumping between words on the terminal.
unbind-key -n M-Left
unbind-key -n M-Right

# Press <prefix> and then 'a' to send Ctrl-a to the application
bind a send-prefix

# These bindings allow you to stay in your keyboard's home
# row when moving between panes. Assuming, of course, that
# you've swapped your Ctrl and Caps Lock keys which you should!
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R


bind-key -n M-h previous-window
bind-key -n M-l next-window
bind-key -n M-j switch-client -n
bind-key -n M-k switch-client -p

# split panes using v and h
unbind v
unbind h
unbind % # Split vertically
unbind '"' # Split horizontally
bind v split-window -h -c "#{pane_current_path}"
bind h split-window -v -c "#{pane_current_path}"


# Create new Window
unbind t #DEFAULT KEY: Show Clock
bind t new-window -c "#{pane_current_path}"
# Rename Window
unbind n
bind n command-prompt "rename-window '%%'"

# Make copy/paste same as vim
bind Escape copy-mode
unbind p
bind p paste-buffer
bind-key -T copy-mode-vi 'v' send-keys -X begin-selection
bind-key -T copy-mode-vi 'y' send-keys -X copy-selection-and-cancel


bind -n 'M-,' display-popup -w 80% -h 80% -E "chezmoi edit ~/.config/zsh/.zshrc && chezmoi apply"
bind -n 'M-.' display-popup -w 80% -h 80% -E "tmux attach -t popup || tmux new -s popup"

# Switch Sessions
bind Tab choose-session

# Kill current pane with "C-w q"
unbind x
bind q kill-pane
# Show and Hide Bar
bind C-b set status


# Nested TMUX 
# --- F12 TOGGLE CONFIGURATION ---

# 1. When F12 is pressed: Disable prefix, dim the status bar, set key table to 'off'
bind -T root F12  \
  set prefix None \;\
  set key-table off \;\
  set status-style 'bg=colour235 fg=colour238' \;\
  set window-status-current-format ' #[fg=colour240]#W ' \;\
  set window-status-current-style 'fg=colour240 bg=colour236 bold' \;\
  if -F '#{pane_in_mode}' 'send-keys -X cancel' \;\
  refresh-client -S

# 2. When F12 is pressed again (while in 'off' mode): Restore everything
bind -T off F12 \
  set -u prefix \;\
  set -u key-table \;\
  set -u status-style \;\
  set -u window-status-current-format \;\
  set -u window-status-current-style \;\
  refresh-client -S

